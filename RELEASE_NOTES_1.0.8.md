# Newbili 1.0.8 · Android 内容库与播放器设置

Android 版本：1.0.8（11），最低 Android 12。本轮仅更新 Android；iOS/iPadOS 继续使用 [v1.0.7](https://github.com/Rseam-07/Newbili/releases/tag/v1.0.7) 的包，没有把旧 IPA 改名重发。

## 本轮改动

- 观看记录、稍后再看、收藏夹及相关搜索页共用按 iOS 内容库布局实现的卡片：92×58 封面、标题、作者、时间或统计、观看进度，保留批选、删除、搜索、翻页及不同内容类型的播放入口。
- 内容库按实际内容与字号自然增高；收藏夹排序同步采用自适应高度。首页依然默认双列，本轮没有改成单列首页。
- 播放设置采用分组卡片，把后台播放、听视频、定时关闭和播放顺序放到首组；左右/上下翻转改为整行开关。保留画质、音质、解码、超分、弹幕、字幕、缓存、CDN、播放信息和举报等入口。
- 内容库操作按钮至少 48dp，支持长按批选；菜单点击不会同时打开视频。
- 稍后再看删除按视频 ID 匹配当前列表，避免请求期间刷新或重排导致删错条目；收藏夹删除同样按 ID 定位。
- 修正文件 Provider 的 authority，让独立 Debug 包能与测试渠道 Release 包共存，无需卸载旧包或清除账号数据。
- 修复 GitHub Linux 构建脚本的解释器兼容问题。候选包经分析和测试后构建，再在本地使用原有测试渠道证书签名。

## 验证状态

本文件为候选版本记录，当前尚未发布。测试、构建和产物签名核验完成后，补充本节的实际结果；不沿用上一版本的测试数字。

仍需实体设备与真实账号验收收藏/历史同步、点赞提交、下载、锁屏音频、蓝牙、PiP 和长时间滚动/播放。不能将本轮描述为全 App 已与 iOS 完全一致。
