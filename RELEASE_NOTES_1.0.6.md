# Newbili 1.0.6 (7)

- 首页提供“新版首页”“影像杂志”“简约模式”三套布局：焦点推荐支持自动轮播、手势切换和播放控制；影像杂志采用独立的 OLED 内容编排，iPhone 与 iPad 的频道导航改为屏幕几何居中，滚动收起加入迟滞，减少抖动和遮挡。
- 播放器补齐手动画中画入口、听视频与后台播放偏好、后台/前台生命周期恢复及会话复用；返回视频详情时优先延续已有播放器、进度和当前播放上下文，减少重复创建与重新加载。
- 弹幕能力重构为可复用的过滤、显示和交互链路：支持点击弹幕、点赞、复制、本地屏蔽与举报，补齐按类型、颜色、等级、关键词、用户和运动方式的设置；游客也可以使用不依赖登录的本地操作。
- 评论、排序、头像、重试与“查看更多”等交互区域统一达到至少 44pt；一键三连继续使用长按进度、阶段震动和取消保护，降低滚动或连点误操作。
- 设置页补齐首页、视频详情、弹幕、播放与 Apple Intelligence 入口，并加入可搜索索引；移除没有实际落地页面的搜索结果，避免出现“能搜到但不能用”的假入口。
- 新增设备端视频总结实验入口：优先读取字幕并在支持的设备上使用 Foundation Models，本地处理且默认关闭；字幕缓存和并发任务按账号身份隔离，不保存原始 Cookie。
- App Intents 补齐首页、搜索、继续播放和视频总结等系统入口；不可用能力会给出明确说明，不伪装成已经运行成功。
- 网络互动统一增加非幂等请求防重：并发三连、收藏、评论、发弹幕和举报共享同一任务结果，失败不自动重复提交，但允许用户明确重试；敏感文本只参与 SHA-256 去重材料，不以明文写入键或缓存。
- 性能方面继续减少首页启动预取、视频详情后台侧载和播放器重复创建；图片预取窗口、弹幕渲染窗口及状态更新均加入去重或合并策略，保留功能的同时降低滑动和播放过程中的无效工作。
- 验证结果：iOS/iPadOS Debug 全量回归共 640 项，639 通过、0 失败、1 项因模拟器不支持系统画中画而跳过；Release IPA 已验证为 `1.0.6 (7)`、arm64、压缩结构完整且未签名。另在 iPhone 17e 与 11 英寸 iPad Pro 模拟器检查了三套首页布局和导航居中。

本版仅发布 iOS/iPadOS，Android 源码未更新，因此不附带 APK。真实账号互动、系统画中画、锁屏后台音频、耳机中断、震动反馈及设备端 Foundation Models 仍需在支持的实体设备上继续验收。IPA 为未签名包，需自行签名后安装。

## SHA-256

- `Newbili-1.0.6-7-unsigned.ipa`: `b79eae2e0e25b3319f086d5cd72758e0c4db5ce660b76212a17322e600625fc0`
