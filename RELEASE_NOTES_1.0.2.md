# Newbili 1.0.2 (3)

- 优化首页滚动、播放器与弹幕渲染性能，不删减动画、玻璃效果或任何现有功能。
- 新版首页不再在 SwiftUI 刷新时重复复制视频列表；番剧、影视列表同步移除无意义的临时数组分配。
- 音频浮动播放模块改为只订阅播放、下一首和内容切换等相关状态，避免评论、弹幕等无关更新触发根视图刷新。
- 弹幕逐帧回收复用缓冲区，并缓存低电量/温度环境判断，降低长按倍速和高密度弹幕时的主线程开销。
- 图片内存缓存的动态预算判断改为限频执行，避免每次图片命中都重复查询系统状态。
- 实时玻璃光晕保持默认开启并完整保留原模糊参数；设置 → 首页与搜索新增独立开关，只有用户主动关闭时才使用低开销渐变光晕。
- 修正媒体服务重置测试的固定等待竞态，改为有超时上限的条件等待；完整回归 493 项，0 失败、1 项按既有条件跳过。
