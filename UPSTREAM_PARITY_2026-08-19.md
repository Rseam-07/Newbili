# PiliPlus 功能对照基线（2026-08-19）

## 基线

- 上游仓库：`bggRGjQaUbCoE/PiliPlus`
- 对照分支：`main`
- 对照提交：`b11815c85c6a3a34a9419e5a0d0ec26687595c0c`
- 提交时间：`2026-08-18T10:32:01+08:00`
- 本项目继续遵循上游 GPL-3.0 许可证，不把上游 Flutter 页面直接嵌进 iOS；接口语义、字段和交互行为可按许可证移植，界面优先用 SwiftUI、UIKit、AVFoundation 等原生能力重建。

## 本轮确认完成

- 稍后再看完整闭环：列表、全部/未看筛选、搜索、新旧排序、分页、添加、单条移除、批量移除、清理失效/已看/全部。
- 稍后再看普通视频与 PGC 条目的路由、播放进度、发布日期和嵌套番剧字段解码。
- 视频详情可通过收藏按钮长按菜单直接加入稍后再看。
- 登录用户等级：解析导航接口的 `level_info`，兼容数字/字符串字段并在“我的”头像区显示 LV0-LV6。
- 播放控件模式：默认自写控件；设置中可切换 iOS 原生播放控件。自写模式继续保留弹幕、全屏手势与应用内玻璃控件。
- 自动化验收入口：可用 `--start-mine-route watchlater` 直达稍后再看页。

## 已有的主要功能闭环

- 首页推荐、热门、排行榜、搜索与多类型搜索结果。
- 视频详情、分 P、清晰度/编码/线路选择、续播、投币、点赞、收藏、评论、弹幕、SponsorBlock、画中画、听视频。
- 动态流、动态详情、评论与互动；UP 主主页、投稿、动态、合集/系列。
- 直播首页、分区、搜索、房间播放与直播弹幕。
- 二维码、短信、网页登录，多账号实验；观看记录、收藏夹、稍后再看、账号消息与私信基础界面。
- iOS 原生导航、标签页、菜单、表单、搜索、材质与 Liquid Glass 风格适配。

## 仍需继续补齐的上游功能

以下项目不能因为已有相近页面就标记为“一比一完成”：

- PGC：完整番剧索引、筛选、评分、追番管理以及会员购/课程等边界页面。
- 私信：完整会话行为、屏蔽设置、二级页和异常状态覆盖。
- 离线下载：任务队列、分 P/番剧下载、后台续传、磁盘管理与本地播放。
- WebDAV：配置、上传/同步、冲突处理与错误恢复。
- DLNA：设备发现、投屏控制、进度同步与断线恢复。
- 专栏/文章：完整阅读器、文章列表、评论与作者文章页；当前部分结果仍依赖网页路由。
- 独立音频：音频主页、歌单/合集、UP 主音频与后台音频队列；“听视频”不等同于此模块。
- 账号管理：黑名单、登录设备、登录记录、硬币记录、经验记录。
- 弹幕管理：屏蔽词/用户管理、直播弹幕屏蔽页与更完整的高级规则。
- 动态发布：图文、投票、预约、话题选择、@ 提及、转发编辑完整流程。
- 关注与收藏管理：关注标签、排序、搜索、收藏夹创建/排序等工具页。
- 其余上游长尾页面及所有登录态错误分支、风控状态和空数据状态。

## 静态审计口径

- 上游 `lib/pages` 当前包含 114 个一级页面目录；这只是功能入口数量，不等同于 114 个独立业务闭环。
- 对上游 `lib/http/api.dart` 与本项目 Swift 服务层进行字面量 URL 粗略比对：上游约 219 个，本项目约 86 个，完全相同约 53 个。
- 该 URL 统计会漏掉动态拼接、复用客户端和非 HTTP 功能，只用于发现缺口，不能作为完成率。
- 每个模块只有在模型、接口、原生页面、导航、加载/错误/空状态、登录态以及测试均闭环后才会从“待补齐”移出。
