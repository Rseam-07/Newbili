# Newbili 1.0.6v2 (8)

本次为仅面向 iOS / iPadOS 的稳定性修正版本；Android 不发布，也没有重新打包 APK。

## 首页与界面

- 首页内容主体恢复到 1.0.5 的稳定布局，撤下 Fluent UI 和第三套首页。
- 重做首页顶栏：频道选择器按可见内容区域真正居中，加入随时间和账号名称变化的问候语；同时适配 iPhone / iPad、安全区、横屏、辅助字号、减少动态效果和降低透明度。
- 顶栏不再使用整条灰色材质胶囊，仅用轻量渐变承接内容，并在当前频道上保留小范围强调和按压反馈。

## 播放与弹幕

- 修复弹幕颜色异常：保留服务端 RGB 文字颜色，并按亮度选择黑色或白色对比描边，不再把弹幕渲染成一片黑色。
- 保留播放器表面复用和滚动图片加载抑制等性能路径，减少返回视频页后的无效重建和重复加载。

## 通知与追更

- 新增关注 UP 更新通知，可选择关闭、仅特别关注或全部关注；通知权限只在用户主动操作时申请。
- 新增“我的追更”列表，集中展示用户手动标记的番剧，可直接回到对应视频。
- 对已标记番剧记录分 P 快照；后台检查发现新增分 P 时发送本地通知，误删追更项目后可在 5 秒内撤销。
- 通知检查强制请求新网络数据，并在关闭后重新开启时重建基线，避免补发停用期间的旧投稿。
- 通知设置提供权限状态、系统设置入口和未登录提示；通知点击可回到对应视频。

## 品牌与兼容

- 清理搜索设置、SponsorBlock、推荐和播放诊断中的用户可见 PiliPlus 字样，统一为 Newbili 表述。
- 旧的第三首页和 Fluent UI 偏好会在升级时安全迁移，不影响已有用户启动。

## 说明

- 关注和追更提醒使用 iOS 本地后台刷新。系统会根据电量、网络和使用习惯决定执行时机，因此不能承诺像服务端推送一样在更新瞬间必达。
- IPA 未签名，需要自行签名后安装。

## 验证

- Xcode 27.0、iPhone 17 Pro（iOS 26.5）模拟器串行执行 658 项测试：657 通过、0 失败、1 项因模拟器不支持系统画中画而跳过。
- 已在 iPhone 17 Pro 和 iPad Pro 11 英寸模拟器视觉核验首页顶栏、问候语、频道居中和安全区布局。
- `Newbili-1.0.6v2-8-unsigned.ipa` 已核验为 `com.rseam07.newbili`、`1.0.6 (8)`、arm64、压缩结构完整且未签名，文件大小 19,337,096 字节。

## SHA-256

- `Newbili-1.0.6v2-8-unsigned.ipa`: `d61e96d953a0b2253cf06a08eed226fe1747aefad9f7341a87445e422317bd4b`
