# Newbili 1.0.7 · Android 向 iOS 对齐与性能更新

版本：1.0.7（10）。本次同时提供 iOS/iPadOS 未签名 IPA 与 Android 测试渠道 APK；不恢复已撤回的 1.1.0。

## Android

- 首页对齐 iOS：居中分类、问候语、可暂停的自动推荐轮播、默认双列、五项玻璃胶囊导航。保留用户已经保存的布局与排序偏好。
- 重排个人页、登录入口、分组设置、双列热词、搜索结果、视频操作条和详情/评论导航。扩大点赞、菜单、筛选等按钮的触控区域，补充状态反馈和减弱动画适配。
- 搜索结果使用紧凑卡片与底部筛选胶囊；保留所有搜索类型、排序、时长/分区/日期筛选、专属内容标签、进度和更多操作。
- 新增关注 UP 通知三档：关闭、仅特别关注、全部关注。首次检查只建立基线；切换账号隔离通知历史，关闭后清除后台凭据。
- 视频更多菜单新增“标记为番剧 / 追更”；“我的 → 我的追更”保存列表并检查新增分 P。按 CID 去重，重排和删除不误报，取消追更撤销对应通知。
- 后台播放对齐“关闭 / 仅听视频 / 始终继续”，默认仅听视频；已有开关偏好自动迁移，回到页面复用播放器。
- 复用现有完整 Flutter 业务与播放器，没有另建删减功能的跨端演示壳。共享导航、分组、封面、后台选择和请求状态处理，清理重复实现。
- 实时模糊集中于固定导航、播放器和浮层，滚动卡片不逐项模糊；轮播在离屏、后台、操作中或减少动画时暂停。后台通知不启动第二个 Flutter 引擎，并发检查共享结果。

## iOS / iPadOS

- 首页默认双列，已有手动偏好不被覆盖。
- 优化密集弹幕跳转后的重放选取、文字测量缓存与标签申请时机；保留对象池及白色/彩色字心的描边修复。
- 合并共享只读请求的完成路径，减少额外分配；单个调用取消不打断其他共享请求，失败后可重试。
- 未新增或恢复第三套首页、Fluent 首页。

## 安装

- iOS/iPadOS 26.4+：`Newbili-1.0.7-10-unsigned.ipa`，需要自行签名后安装，不是 TestFlight 包。
- Android 12+：普通手机优先下载 `arm64-v8a-test.apk`；另提供 `armeabi-v7a` 和 `x86_64`。APK 是优化后的 Release 构建，沿用既有公开测试渠道证书，包名 `com.rseam07.newbili`，版本号 10。`test` 表示测试签名，不表示 Debug 运行模式。
- 可覆盖相同证书、较低 versionCode 的旧测试包。不要为了安装而先删除旧客户端和账号数据。
- 下载后可用同页 `SHA256SUMS-1.0.7.txt` 校验文件。

## 验证与已知限制

- iOS 全量模拟器回归：667 项，666 通过、0 失败、1 跳过（系统 PiP 不支持）。Release IPA 单独构建并核验版本与包完整性。
- Android：63 项 Flutter 测试、9 项原生单元测试、7 项 Android 12 设备通知检查通过；静态分析无 error/warning，保留 33 项上游 info。
- Android 12 模拟器已走真实首页、搜索、视频取流、白色描边弹幕、评论、后台进度保留、标记追更和公开 API 检查。通知投递/取消、失败重试、特别关注过滤、账号隔离与并发合并的设备检查使用隔离数据及合成 API 响应。
- 通知是本地周期检查，不是服务端实时推送。后台最短周期 15 分钟，并可能因省电、网络和系统策略延后；没有“更新即刻到达”保证。首次启用不会推送旧投稿。[Android 调度说明](https://developer.android.com/develop/background-work/background-tasks/persistent/getting-started/define-work)
- 尚未完成所有深层播放器设置、账号页面与直播间的逐像素对齐；Android 仍使用 B 站 AI 总结，不具备 Apple Intelligence 本地总结。不能将本版描述为全 App 已完全一致。
- 模拟器验证不等于实体手机性能验收；真实登录投稿通知、下载、锁屏音频、蓝牙、PiP、不同厂商省电策略和长时间滚动/播放还需要真机验证，不承诺卡顿已全部消除。

更细的实现与证据范围见仓库 `PERFORMANCE_PARITY_2026-09-05.md`。
