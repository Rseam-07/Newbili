# Newbili 1.0.6v3 (9)

本次为仅面向 iOS / iPadOS 的弹幕紧急修正版本；Android 不发布，也没有重新打包 APK。

## 弹幕修复

- 修复部分真机上白色和彩色弹幕被对比描边覆盖、最终显示成黑色文字的问题。
- 将弹幕文字改为两阶段绘制：先绘制对比轮廓，再独立覆盖服务端 RGB 前景色，粗描边也不会吞掉文字填充。
- 保留弹幕标签对象池、尺寸缓存和图层栅格缓存；样式只在弹幕创建或设置变化时生成，不增加逐帧字符串与布局开销。
- 加入用户报告视频的真实 protobuf 弹幕样本回归，确认“那就不是本人了”解析为 `0xFFFFFF`。
- 加入最大 5pt 描边的像素级回归，直接验证渲染结果同时包含白色填充和黑色轮廓，不再只检查颜色属性。

## 说明

- 本版本延续 1.0.6v2 的首页、通知、追更和 iOS 播放改进。
- IPA 未签名，需要自行签名后安装。

## 验证

- Xcode 27.0、iPhone 17 Pro（iOS 26.5）模拟器执行 660 项测试：659 通过、0 失败、1 项因模拟器不支持系统画中画而跳过。
- 弹幕专项 45 项全部通过，覆盖真实 protobuf 颜色、位图像素、设置、旋转重建、点击交互与实时弹幕。
- `Newbili-1.0.6v3-9-unsigned.ipa` 已核验为 `com.rseam07.newbili`、`1.0.6 (9)`、arm64、压缩结构完整且未签名，文件大小 19,336,701 字节。

## SHA-256

- `Newbili-1.0.6v3-9-unsigned.ipa`: `c3ef80d8a811168a4d74b7346816c32ccb97d65d54eabf42b2798cab32ca1510`
