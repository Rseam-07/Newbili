# Android 操作可靠性检查 · 2026-09-15

本记录对应发版前的 AndroidFlutter 视频页、弹幕发布、共用颜色选择器和 HTTP 重试修改；未修改 iOS。后续打包与发布状态见 [1.0.9 版本说明](RELEASE_NOTES_1.0.9.md)。

## 本轮修正

- 发弹幕和弹幕显示开关从更多菜单中移出，均可直接点击；常规视频页的相关按钮扩大到 48dp。大字体下操作栏换行并保留底部内容空间。
- 弹幕编辑器显示固定发送时间，输入、样式、颜色与发送状态分工明确。保留小/标准字号、滚动/顶端/底端、14 种预设颜色、自定义颜色和会员渐变色。
- 发送区固定在键盘上方，内容与样式单独滚动。快速点击、键盘发送共用同一个发送锁；发送期间不能修改内容或意外关掉编辑器。普通关闭和失败都保留当前视频页的草稿，确认成功后才清除。
- 每次请求绑定打开编辑器时的视频、分 P、时间和账号。未开始播放的当前视频按 00:00 发送，不借用后台其他视频的进度；本地回显和自动恢复播放另行检查播放器是否仍属于原视频。
- 发送异常在编辑器中说明，移除全局加载遮罩；服务器结果不确定时提示先检查再试，不自动补发。增加发送超时，容忍可选弹幕 ID 的数字/字符串/缺省返回。
- 网络连接错误不再自动重放 POST/PUT/PATCH/DELETE。GET/HEAD/OPTIONS 保留有次数上限的读取重试；原有重定向处理未改变。这降低了发弹幕、评论、投币等写入操作被重复执行的风险，但不是服务器端 exactly-once 保证。
- 复用的颜色选择器只关闭自身所在弹窗，不再调用全局返回而误关其他页面。

## 实现取舍

按 ui-ux-pro-max 的表单反馈、48dp 点击区域、键盘安全区、无障碍标签和减少动态效果规则检查。此次没有引入新的 UI 框架、重写播放器或增加每帧特效；编辑器移除不需要的 Rx 状态和聊天面板生命周期，仅复用现有 HTTP、弹幕绘制和颜色选择器。输入时只更新发送按钮和输入框自身状态。

返回拦截遵循 [Flutter PopScope](https://api.flutter.dev/flutter/widgets/PopScope-class.html) 的局部路由机制。与本地上游 PiliPlus 的 `send_danmaku/view.dart`、视频页操作入口进行了对照，没有删掉其弹幕样式能力。

## 验证与边界

- 新增网络重试测试 25 项、编辑器行为/布局测试 11 项。覆盖重复提交、失败后主动重试、空白输入、草稿与样式恢复、发送中返回、销毁后延迟回包、自定义颜色确认、横屏键盘、大字体、明暗主题、减少动态效果。
- 全量 Flutter 回归 112 项通过；静态分析无 error/warning，保留上游已有的 info 提示。
- 渲染检查包含 375×812、812×375、320×812（2 倍字）、1024×768（3 倍字）。横屏初稿暴露发送按钮可滚出视野的问题，已改为固定操作区，并增加无需滚动即可点击的断言。
- 本地迁移目录存在阻塞读取，测试使用公开提交 `74fee04386bfcb3b7428457b6aae9ed108f84eb7` 的隔离副本叠加本轮列出的改动；交付前逐文件比较副本与原目录的一致性。原目录其他文件与可能存在的同步副本未被覆盖或清理。
- 以上是代码、Widget/单元测试及组件渲染证据，不是 Android 真机输入法、真实账号写入、后台切换、长时间播放或帧率基准。未向用户账号发送测试弹幕或执行投币等外部写入，也不宣称所有使用问题已解决。

验证日志保存在本机 `/private/tmp/newbili-usability-full-tests.log`、`/private/tmp/newbili-usability-final-analyze.log`；组件渲染在 `/private/tmp/newbili-composer-visuals/`。这些临时文件不是安装包。
