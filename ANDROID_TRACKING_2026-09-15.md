# Android 追更功能补齐 · 2026-09-15

本记录对应发版前在现有 `AndroidFlutter` 主线上继续的追更修改；没有重建客户端，没有变更 iOS。后续打包与发布状态见 [1.0.9 版本说明](RELEASE_NOTES_1.0.9.md)。

## 已补功能

- **我的追更搜索**：支持视频标题、UP 主、BV 号，忽略英文大小写；本地筛选不发网络请求，清空搜索恢复列表。空列表与无匹配结果分别提示。
- **追更选集**：菜单中可打开分 P 列表、按集数/标题搜索、正序/倒序及播放最后一 P。播放使用所选 CID，不按易变的列表下标猜测分 P；复用现有播放器导航、网络与播放逻辑。
- **取消追更撤销**：保留确认框；原生存储在同一个锁内删除并返回当时的完整快照，撤销恢复原标记时间、检查时间及已知 CID。重复撤销不会覆盖新加入的同一视频，也不会把历史分 P 当成新更新。当前页面可撤销最近一次取消；离开页面后不保留撤销入口。
- **无系统通知权限也能手动检查**：手动检查继续更新 App 内追更快照和最近更新；不会绕过系统权限发送通知。后台调度仍遵守通知开关和系统限制，UP 检查仍遵守用户选择的档位及登录状态。首次同步不补发历史。

## 可靠性与复用

- 删除/恢复按 BVID 防重入；操作失败保留现有列表或撤销快照，允许明确重试。
- 采用原生状态修订号，丢弃晚到的旧快照；不会因检查和编辑交错，重新显示已经取消的追更。
- 列表复用 `NewbiliLibraryTile`，选集使用惰性列表；未增加依赖、第二个播放器、额外 Flutter 引擎、逐卡片模糊或轮询定时器。
- 按 ui-ux-pro-max 的触控、状态恢复、键盘和大字号检查调整交互。横屏/大字号/键盘挤占高度时压缩选集头部，保留搜索、排序、关闭按钮与可滚动分 P 区；聚焦输入框使用语义颜色，避免浅色品牌粉造成低对比度。

## 验证及边界

- Flutter 全量回归：125 项通过，包括上一轮弹幕编辑器、防重复请求与既有内容库测试。
- Dart 静态分析：无 error/warning，保留 33 项既有 info；本轮 11 个源码/测试文件与隔离验证目录逐文件比对一致，已跟踪文件的差异格式检查通过。
- 本轮新增 13 项 Flutter 测试：快照往返、删除/撤销失败恢复、重复点击、旧响应乱序、通知关闭后手动检查、搜索、选集 CID 与弹层返回、5 组自适应布局。
- Kotlin/JUnit 追更策略：12 项通过，含撤销保留历史 CID、旧数据迁移、手动/后台权限决策。
- 原生追更监控、调度、存储与设备测试代码通过 Kotlin 编译检查；使用本机 Android API jar，应用入口和生成的资源常量仅使用编译占位符。这不是完整 APK 构建，也不是设备运行结果。
- 新增实际存储撤销的 Android instrumentation 用例，但本轮没有运行设备 instrumentation。
- 已使用真实中文字体渲染并人工检查浅色、深色、375dp、横屏、320dp 两倍字号和平板三倍字号；键盘为布局插入量，图片为组件测试渲染，不是实体设备截图。
- **尚未验证**：真实账号等到 UP 投稿的后台端到端、厂商省电限制、设备上通知权限切换、重启后调度及真机播放体验。本轮不宣称全 App 已与 iOS 完全一致。

验证日志：`/private/tmp/newbili-tracking-full-tests.log`、`/private/tmp/newbili-tracking-tests.log`、`/private/tmp/newbili-tracking-analyze.log`、`/private/tmp/newbili-tracking-native-compile.log`。渲染位于 `/private/tmp/newbili-tracking-visuals/`。
