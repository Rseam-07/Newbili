# 性能、可靠性与跨端对齐记录 · 2026-09-05

基线：`64ff323`（iOS `v1.0.6v3`）。本次在现有 Swift iOS 与完整的 `AndroidFlutter` 业务代码上改动，没有另建播放器或删减上游业务。以下区分代码实现、测试通过和仍未完成的端到端对齐；本记录不是发版声明。

## 本轮修改

### iOS

- 弹幕跳转后的重放窗口反向选取最新有效项，达到显示预算即停止；不再对每条候选执行数组头部删除。保留时间顺序，过期、未来和不支持的模式不会占用预算。
- 排版与绘制共用原始文字尺寸缓存，移除 `drawText` 中的重复测量。统一两处标签尺寸计算；没有可用轨道时不再申请、配置、回收一个不会显示的标签。
- 保留对象池、光栅化、时间同步和“先描边、后服务端 RGB 填充”的修复，不修改用户弹幕设置。
- 共享只读请求合并为单一完成/错误路径，去掉没有替换路径却每次分配的 UUID 包装。取消的调用方不能继续拿到旧响应；共享请求不会被其中一位调用方的取消打断。请求失败后可再次尝试。

### Android

- 默认品牌色对齐 iOS `#EE719E`；保持用户明确选择的动态取色和颜色。首页复用问候语时间分段，分类居中，去掉整条灰色玻璃背景。
- 默认导航为：首页、动态、直播、我的、搜索。新增枚举项追加到原序号之后，保存过的排序不会被重新解释。首页分类和底部导航共用持久化序号校验。
- 复用已有直播与搜索业务页面；直播控制器分离 root/home 实例；搜索保留 tab 状态并释放独立控制器；切换 tab 收起键盘。
- 使用共享胶囊导航组件替代约 780 行复制的导航实现，保留语义、焦点、触摸反馈和减弱动画支持。根导航和播放器详情/评论使用同一组件。
- 列表卡片不再各自监听玻璃能力、不做背景模糊或额外光晕；固定导航、工具条、播放器和弹层仍按设备能力显示玻璃。仅首页保留静态氛围渐变，清除未再使用的主题渐变、光晕字段和对应分配。
- 轮播在离屏、切换 tab/路由、后台、拖动、辅助导航或减弱动画时暂停，并提供手动暂停按钮。时间器改为单次调度，页面动画结束后才安排下一次。
- 推荐列表不再在 item builder 中触发请求，加入显式加载入口，消除短列表被轮播占满后无法继续翻页的路径；合并重复的卡片删除逻辑。
- 评论点赞/点踩的假防重入标记改为组件持有的互斥状态；共用请求处理和计数转换，更新捕获的原评论而不是可能已复用的行。按钮扩大到 48dp，增加处理中与状态动画。
- 共享列表加载状态由 `finally` 统一释放，异常不会把刷新永久锁住。必要的登录、权限、参数边界和错误处理仍保留。
- 首页以 iOS 的实际尺寸与层级重排：20pt 问候语、居中分类、270dp 轮播、白色观看按钮、默认双列信息流；保留用户显式保存的密度偏好。
- 个人页、设置入口使用统一的 24dp 圆角分组、行与路由映射；验证码、扫码和其他登录方式直达对应流程。去除个人页已不显示却仍请求的收藏夹预加载。
- 搜索输入与双列热词按 iOS 重排；用标准惰性 SliverGrid 替代复制的自定义渲染器，保留推荐词、直播标记、搜索历史和完整关键词点击。
- 竖屏视频页采用头像、关注、点赞、投币、收藏、分享的紧凑操作条；点踩与稍后再看移入更多菜单，原请求、长按收藏和三连逻辑不变。AI 总结及弹幕入口保证至少 48dp 命中范围。
- 固定结构的背景组件避免切换根 tab 时销毁页面；有组件回归覆盖“选中个人页但内容退回首页”的问题。悬浮导航的留白按字号和系统安全区计算。
- 播放器白色控制图标使用独立深色玻璃材质，避免在浅色主题中白底白字；临时触摸诊断输出已移除。
- 截图复核发现动态页标题/分类组合有 2px 溢出，修正 TabBar 指示器占位和字体行高取整，并补上 1～3 倍字号的组合回归。
- 搜索结果改为紧凑横向卡片和底部玻璃筛选栏，保留六类搜索、排序、时长/分区/日期筛选、标签、历史进度和更多操作。紧凑/旧版卡片共用封面逻辑；翻页移到滚动监听并提供显式按钮。
- 新增“关闭 / 仅特别关注 / 全部关注”的 UP 投稿通知，以及视频更多菜单标记、我的追更列表、新增分 P 通知。首次建立基线，按 CID 判断新增，不把重排/删除当作更新；切换账号重建基线，关闭时清除后台凭据。
- 使用原生 JobScheduler 和小型检查服务，不启动第二个 Flutter 引擎；后台凭据 AES-GCM 加密、密钥由 Android Keystore 包装。前后台并发检查合并，失败保留已知内容，取消追更后不再投递旧检查结果。
- 后台播放对齐“关闭 / 仅听视频 / 始终继续”，新用户默认仅听视频；迁移已有布尔偏好，播放器复用现有实例，PiP 保持可见播放。播放器、直播控制和设置共用选择组件。

玻璃范围、惰性列表和局部重建遵循 [Flutter 性能指导](https://docs.flutter.dev/perf/best-practices)；不把每张卡片都变成背景滤镜。[BackdropFilter 文档](https://api.flutter.dev/flutter/widgets/BackdropFilter-class.html)也说明了滤镜范围与分组的约束。

## 验证

| 项目 | 已取得的证据 |
| --- | --- |
| iOS 全量 Debug 模拟器回归 | 667 项，666 通过、0 失败、1 跳过（系统 PiP 不支持） |
| 弹幕真实样本像素回归 | 最大描边下保留白色字心和深色轮廓；彩色弹幕保留服务端颜色 |
| 5 万条密集弹幕选取基准 | 同一 Debug 模拟器，旧算法平均约 16.6ms；新算法约 0.03ms。属于局部基准，不能外推为真实播放帧率或整机性能倍数 |
| Android Flutter 自动化 | 63 项通过；含排序兼容、问候语、评论状态转换、异常释放、375dp/横屏/平板、1～3 倍字号、双列视频与热词、登录入口、导航保留、弹幕菜单、48dp 触控、播放器对比度、静态卡片无模糊、轮播生命周期、通知状态/失败恢复、后台档位迁移和搜索筛选 |
| Android 原生单元测试 | 9 项通过；CID 去重、新增判断、快照验证、UP 动态解析和特别关注过滤 |
| Android 12 通知设备检查 | 7 项通过；实际 NotificationManager 投递、取消通知、JobScheduler 调度、失败重试、首次基线、账号隔离、加密存储、并发请求合并。使用隔离偏好和合成 API 响应，不是实际账号等待 UP 投稿的验收 |
| Android 静态分析 | 无 error/warning；33 项 info，主要为上游复制组件弃用 API 等，未批量改动其行为 |
| iOS Release 编译 | 1.0.7（10）全量回归后重新构建未签名 IPA；arm64、包名、版本、最低系统版本和 ZIP 完整性已核验 |
| Android APK / Android 12 真正运行 | arm64 Debug 构建并安装到 API 31 模拟器；已实看首页、个人页、搜索、热词到真实搜索结果、直播列表、视频详情、实际视频/白色描边弹幕、评论加载和切页。动态页验证游客状态。不是实体手机性能验收 |
| Android 后台播放 / 返回页面 | 开启后台播放后，媒体会话持续处于播放状态，进度从 591 秒走到 653 秒；回到视频页继续至 654 秒，没有重头加载。默认关闭时退后台暂停、返回继续。模拟器无音频输出，不能替代锁屏/蓝牙真机听感验收 |
| 追更实际 API | 公开视频从更多菜单标记后出现在我的追更，标题/封面/分 P 正确；真实公开 API 检查显示“已检查 1 项，目前没有新内容”，没有把历史分 P 当作新增通知 |

本机日志与完整测试结果：

- `/private/tmp/Newbili-1.0.7-FullTests.xcresult`
- `/private/tmp/newbili-1.0.7-ios-full-tests.log`
- `/private/tmp/newbili-1.0.7-ios-release.log`
- `/private/tmp/newbili-1.0.7-android-tests.log`
- `/private/tmp/newbili-1.0.7-android-analyze.log`
- `/private/tmp/newbili-1.0.7-android-debug-build.log`
- `/private/tmp/newbili-android-native-notification-tests.log`
- `/private/tmp/newbili-android-notification-instrumentation.log`
- `/private/tmp/newbili-android12-background-enabled-before.txt`
- `/private/tmp/newbili-android12-background-enabled-after.txt`
- `/private/tmp/newbili-android12-background-enabled-resumed.txt`

本地界面截图位于 `.codex-screens/android-ios-parity-20260905/`（首页、动态游客态、直播列表、个人页、搜索、视频详情和评论）。该目录的旧搜索结果截图保留作为改版前记录。图片直接来自 Android 12 模拟器，未合成或替换界面。

模拟器使用 SwiftShader 图形后端。先前 auto 图形后端曾在模拟器 `libOpenglCodecCommon.so` 崩溃，切换后实际视频播放未再出现该崩溃；日志仍有模拟器 MediaCodec 的解码回退。不能据此宣称所有真机编解码问题已解决。

## 早期本地预览包（不是 v1.0.7 发布产物）

- 文件：`dist/Newbili-Android-iOSParity-20260905-debug-arm64.apk`
- Android 12 及以上，Flutter 运行库为 arm64；APK 签名校验通过，最终文件已在 API 31 模拟器覆盖安装并启动。
- 包名：`com.rseam07.newbili.debug`，与正式客户端分开安装，不迁移或覆盖正式账号数据。
- 属于 Debug 预览，内部仍沿用工程的 `1.1.0 (8)` 元数据；没有创建 tag、上传 Release 或恢复撤回版本。Debug 包体积和模拟器帧率不能用于发布版性能比较。
- SHA-256：`9061520703494f66ef7987fa2a57956c28224bba4c27ba41d39ce01afd39c67a`

## 尚未达到完全对齐的部分

| 项目 | 当前事实 / 下一步验收 |
| --- | --- |
| UP 更新通知与后台调度 | Android 已实现分档、追更和通知链路；实际登录账号的投稿端到端、OEM 省电和跨重启调度仍需真机长时间验收。系统周期最短 15 分钟，不能承诺“立马通知” |
| 本地 AI 总结 | Android 保留现有 B 站 AI 总结；不是 Apple Intelligence。本地模型能力不能通过更换标签获得，需要单独实现 Android 方案 |
| 设置与二级页面 | 个人页、设置根页、开关、分组、搜索结果筛选已重排；播放器完整设置面板、账号内容二级页、直播间等仍需继续逐页对齐，不能称全 App 一模一样 |
| 后台档位与平台能力 | 三档设置和旧偏好迁移已实现，单元测试验证 PiP 与听视频决策；仍需不同厂商真机的锁屏、耳机中断与系统 PiP 验收 |
| 实际账号与媒体流程 | 模拟器/组件测试不替代真机登录、点赞提交、下载、锁屏音频、蓝牙、PiP 和长时间高密度弹幕验证 |
| 性能最终验收 | 需要真机滚动/播放的帧时间、耗电和内存曲线；目前不声称整机卡顿已消除 |

后续应继续逐个播放器/账号/设置深层流程与 iOS 对照，并进行真实账号和实体设备验收，不能以导航外壳或测试数量代替全 App 功能等价。后台周期约束见 [Android 官方文档](https://developer.android.com/develop/background-work/background-tasks/persistent/getting-started/define-work)。
