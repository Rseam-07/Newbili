# Newbili 1.0.5 (6)

- 首页“每日精选”升级为可自动轮播的焦点推荐：融合当前推荐、热门内容与活动内容，支持手势切换、页码指示和自动播放；进入后台、用户操作、旁白或“减弱动态效果”开启时会主动暂停。iPad 与宽屏窗口采用大幅焦点图和横向内容架，窄屏继续保持紧凑布局。
- 动态点赞改为真实账号接口，加入重复点击拦截、失败回滚和刷新竞态保护；动态评论与回复补齐输入、空内容及 1000 字限制、失败提示和发布成功后的可靠刷新。
- 视频互动扩大可点击区域；“一键三连”支持长按动画、进度反馈与震动，完成后再提交操作，降低误触和重复提交。
- “我的”补齐账号等级经验与进度展示，按账号持久化并在刷新失败时保留最近一次有效数据；历史记录、收藏和稍后再看改为进入对应页面后按需加载，减少首页与启动阶段开销。
- 播放链路保留当前及最近一次会话，返回视频页优先复用播放状态与进度；小窗在账号、清晰度和播放上下文一致时复用现有会话。播放器横竖屏均保留空降助手开关，减少画中画、听视频和后台播放返回时的重复加载。
- 网络层补齐 HTTP 状态、空响应、HLS `Content-Range` / `Content-Length` 校验；投币、评论、弹幕和三连等非幂等请求不再自动重放，避免弱网下重复操作。
- Android 1.0.1 新增扫码登录、跳转哔哩哔哩/复制链接、基于 Android Keystore 的 AES-GCM 会话保存，以及账号资料、等级刷新和退出登录。
- Android 液态玻璃效果在 Android 12 及以上、性能条件允许时使用 Backdrop；低内存、省电模式、用户关闭效果或不支持的设备自动降级为高对比透明材质。图片加载加入目标尺寸解码、同请求合并与缓存，控制内存和重复开销。
- 验证结果：iOS 全量回归共 566 项，565 通过、0 失败、1 项按既有条件跳过；Release IPA 已验证为 arm64、结构完整且未签名。Android 单元测试 11/11 通过，`assembleDebug` 成功，APK v2 调试签名验证通过。

本版重点仍为 iOS/iPadOS。Android 的真实首页数据、动态接口和完整播放器仍在迁移，尚未达到 iOS 功能对等；登录态互动、系统画中画与后台播放等场景仍需使用真实账号和设备继续验证。IPA 为未签名包，需自行签名后安装；APK 使用 Android Debug 证书签名，仅供测试。

## SHA-256

- `Newbili-1.0.5-6-unsigned.ipa`: `fa4a8df4a2986e2a3be641a23019c97ec9f2dec33aed13b100e975e25ebb7465`
- `Newbili-Android-1.0.1-debug.apk`: `960caf8f81e73aa7f3d424ff0e0f17c5a0014a526883dfa5e2f357ddf1dac582`
