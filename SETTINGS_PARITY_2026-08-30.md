# PiliPlus 设置迁移矩阵（iOS，2026-08-30）

## 对照口径

- 上游快照：PiliPlus `44680b8a486a0518f366a2c9bff6242506cf8783`（2026-08-30）。
- 源码入口：`lib/pages/setting/models/style_settings.dart`、`play_settings.dart`、`video_settings.dart`、`recommend_settings.dart`、`extra_settings.dart`、`privacy_settings.dart`。
- “已具备”必须同时有持久化、可到达设置入口、运行时读取和对应交互/加载链路；只有相似页面或搜索关键词不算完成。
- “入口但闭环不足”表示开关确实改变部分行为，但受设备、登录方式或未完成的相邻链路限制，界面必须如实说明。
- 桌面窗口、托盘、Linux SSD、Android 系统亮度/后台小窗等平台专属项不计入 iOS 缺失。

## 源码级分类矩阵

| PiliPlus 分类 | 已具备且运行时生效 | 入口但闭环不足 | iOS 仍缺的高价值项 |
| --- | --- | --- | --- |
| 样式 `style_settings.dart` | 系统/浅色/深色主题、主色调、应用图标、系统/手动字号、图片质量、动态单列/瀑布流、底部 Tab 显隐、滑动缩小 Tab、滚动边缘效果、120Hz、首页风格与布局 | 图片加载诊断属于 Newbili 自有诊断能力，不等同于上游“查看大图质量”；Liquid Glass 风格是增强项，不替代上游全部导航外观选项 | 页面过渡动画、界面缩放、列表宽度、视频页深色主题、图片暗色叠加、Toast 不透明度、默认启动页、首页顶栏独立收起开关、动态 UP 面板位置/展开直播列表、动态与消息未读标记模式、平板侧边栏布局策略 |
| 播放 `play_settings.dart` | 弹幕总开关与主要显示参数、点击弹幕独立开关、默认倍速、进入详情自动播放、画中画设置与自写控件手动入口、后台播放模式、听视频队列顺序、自写/系统播放控件、底部常驻进度条 | 后台播放和锁屏控制已有代码闭环，但仍需真机中断/耳机/锁屏回归；画中画仍需真机启动、返回与系统中断回归；系统原生控件模式会按说明放弃自写弹幕和部分手势 | 全屏锁定/截图/电量按钮配置、双击快进与时长、亮度/音量滑动、滑动快进策略、自动字幕与字幕偏好、竖屏扩大、自动进退全屏、延长控件显示、PiP 屏蔽弹幕、全屏操作按钮、在线观看人数、默认全屏方向、播放器设置仅当前生效 |
| 视频 `video_settings.dart` | 默认/蜂窝画质、编码启用与顺序、硬解优先、杜比视界策略、CDN 线路/自定义 Host/测速、备用音频 URL、IPv4/IPv6 偏好、App/Web 取流来源 | B站定向流量兼容只改变域名优先与失败兜底，无法承诺运营商计费；CDN 弱探测无真实媒体地址时只作参考 | 默认/蜂窝音质、直播默认/蜂窝画质、直播 CDN、蜂窝独立编码顺序、音频输出、缓冲大小/时长、自动同步/视频同步/硬解模式、免登录 1080P 独立策略 |
| 推荐 `recommend_settings.dart` | App/Web 推荐来源、最短时长、最低播放量、最低点赞率、标题关键词、过滤器应用到相关推荐 | 推荐诊断能说明当前凭证与来源，但不等同于上游的刷新位置保存 | 保留首页推荐刷新、上次看到位置提示、分区关键词过滤、已关注 UP 豁免过滤 |
| 其他 `extra_settings.dart` | SponsorBlock 二级设置、屏蔽广告/带货动态与带货评论、动态关键词、相关视频/视频评论/默认展开简介、热搜开关、缓存清理、Apple Intelligence 设备端视频总结及总开关 | 多账号用途分配仍是实验且第二账号仅网页登录；Apple Intelligence 取决于系统模型可用性并仍需支持设备真机验收 | 视频分段/高能进度、番剧评论独立开关、横屏简介/分P/侧栏细项、评论折叠行数、视频/动态争议信息、分P倒序与续播提示、评论关键词、外部浏览器、搜索建议/历史/默认词/搜索发现、快速收藏、震动总开关、评论/动态发布反诈、Live Photo、手势阈值、代理、HTTP/2 与重试、最大缓存、默认评论 Tab |
| 隐私 `privacy_settings.dart` | Newbili 自有无痕模式、游客推荐、多账号用途分配 | 多账号页会解释用途，但尚不是上游“账号模式 API 列表”的逐接口说明 | 黑名单管理、完整账号模式/API 作用说明；更广的登录设备/记录管理仍属于账号模块缺口 |

## 本轮为什么先做这三项

1. 三项均是最新版 PiliPlus 明确存在的高频迁移设置，用户进入视频详情即可感知。
2. Newbili 已有相关视频、评论和普通/番剧简介的真实实现，可直接接入现有 `LibraryStore`，无需复制页面或新增第二套状态。
3. 关闭相关视频/评论不仅隐藏 UI，还会停止对应自动加载，兼顾用户控制、请求稳定性与性能；深链直达评论仍保留，避免设置造成通知链接失效。
4. 默认展开简介以“设置默认值 + 当次手动覆盖”建模，用户手动收起后不会被全局设置反复改回。

## 下一批优先顺序

1. 推荐连续性：保留刷新内容、上次看到位置、已关注 UP 豁免，直接改善首页长期使用体验。
2. 播放器可达性：双击快进时长、延长控件显示、全屏操作按钮、PiP 屏蔽弹幕；需与当前自写播放器统一手势状态，不能只加开关。
3. 搜索迁移：搜索建议、搜索历史、搜索发现与默认词，需先补持久化和隐私边界。
4. 评论可读性：折叠行数、关键词过滤、默认评论 Tab；必须同时覆盖主评论、楼中楼和深链。
5. 账号隐私：黑名单与账号模式逐 API 说明，需要真实接口、登录态和错误恢复后再开放入口。
