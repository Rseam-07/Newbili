# Newbili 1.0.9 · Android 弹幕与追更

Android 版本：1.0.9（12），最低 Android 12。本轮只更新 Android；iOS/iPadOS 继续使用 [v1.0.7](https://github.com/Rseam-07/Newbili/releases/tag/v1.0.7)，没有把旧 IPA 改名重发。

## 更新内容

- 视频页直接显示“发弹幕”和弹幕开关，主要操作至少 48dp；编辑器的发送按钮固定在键盘上方，保留字号、位置、预设/自定义颜色和会员渐变色。
- 连点与键盘发送共用发送锁；普通关闭、失败都保留草稿。提交绑定打开时的视频、分 P、时间及账号，避免切换视频后误发或误恢复后台播放器。
- 网络中断不自动重放 POST/PUT/PATCH/DELETE；发送结果不确定时明确提示先检查再重试。成功响应可兼容数字、字符串或缺省弹幕 ID。
- 我的追更支持按标题、UP 主或 BV 号搜索；支持分 P 搜索、正序/倒序和播放最后一 P，选集按 CID 跳转而非易变下标。
- 取消追更保留确认，并支持在当前页面撤销最近一次取消；恢复原标记/检查时间和历史 CID，失败可重试，不把旧分 P 误报成更新。
- 关闭系统通知后仍可手动检查，更新保存在 App 内，不发送系统提醒。后台调度仍遵守用户档位、登录状态与系统限制，不承诺即时推送。
- 旧响应不会覆盖新的追更状态；列表复用现有内容库卡片和播放器，没有新增 UI 框架或额外播放实例。优化横屏、键盘、大字号及明暗主题的可操作性。

## 安装与升级

- 普通 Android 手机优先下载 `Newbili-Android-1.0.9-12-arm64-v8a-test.apk`；另提供 `armeabi-v7a` 和 `x86_64`。
- `test` 指既有公开测试渠道签名；安装包为启用优化的 Release 构建，不是 Debug 运行模式。包名保持 `com.rseam07.newbili`，versionCode 从 11 升至 12，沿用原测试证书。
- 相同证书的旧测试包可覆盖升级，无需先卸载或清除账号数据。校验文件见同页 `SHA256SUMS-1.0.9.txt`。

## 验证状态

发布候选正在进行最终 CI 编译、测试和本地签名核验；全部完成后补入结果再发布。

本轮仍不替代真实账号和实体设备验收：实际弹幕提交、系统输入法、后台更新送达、OEM 省电、锁屏音频、蓝牙、PiP 及长时间播放/滚动。不能将单元测试或构建成功表述为全 App 已完全对齐 iOS。
